@model IEnumerable<Babadzaki.Models.Token>
<div class="row" id="tokenCardGalleryCount" value="@Model.Count()">
@if (Model.Count() > 0)
{

    @foreach (var token in Model)
    {
        <partial name="_IndividualTokenCard" model="token" />

        //display all product
    }

}
else
{
    <p> No tokens exists.</p>
}

</div>